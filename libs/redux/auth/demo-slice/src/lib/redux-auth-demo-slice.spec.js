import { reduxAuthDemoSlice } from './redux-auth-demo-slice';
describe('reduxAuthDemoSlice', () => {
  it('should work', () => {
    expect(reduxAuthDemoSlice()).toEqual('redux-auth-demo-slice');
  });
});
